apiVersion: v1
kind: Service
metadata:
  name: {{ include "almost-automated-api.fullname" . }}
  labels:
    {{- include "almost-automated-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "almost-automated-api.selectorLabels" . | nindent 4 }}
